# Migracion Fixed

Add minimal SQL and migration steps here as you find DB schema issues; *do not import full migrations from the main `supabase/migrations`* folder.

Examples:
- Create `profiles` table
- Test `auth` RLS policies

When ready to reset DB on Supabase, ensure we have a backup and that you confirm the reset.
